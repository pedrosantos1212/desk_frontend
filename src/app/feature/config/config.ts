import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  imports: [RouterLink],
  selector: 'app-config',
  styleUrl: './config.css',
  templateUrl: './config.html',
})
export class Config {}
